services:
  date:
    image: aspiregray/result-date
    build:
      dockerfile: date.dockerfile
  result:
    image: aspiregray/result
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    depends_on:
      - date
